[package]
name = "firstcut"
version = "1.0.0"
description = "FirstCut — offline street photography grading & sequencing"
authors = ["FirstCut"]
edition = "2021"

[lib]
name = "firstcut_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "firstcut"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri          = { version = "2", features = ["devtools"] }
tauri-plugin-shell  = { version = "2" }
tauri-plugin-dialog = { version = "2" }
serde          = { version = "1", features = ["derive"] }
serde_json     = "1"
ureq           = "2"
reqwest        = { version = "0.12", features = ["json"] }
zip            = { version = "2", default-features = false, features = ["deflate"] }

[profile.release]
panic   = "abort"
codegen-units = 1
lto     = true
opt-level = "s"
strip   = true
